# Partial configuration with only some fields
unqualified-search-registries = ["docker.io"]

# Only registry entries, no credential helpers or aliases
[[registry]]
prefix = "docker.io"

[[registry]]
prefix = "gcr.io"
location = "gcr.io"
insecure = false